# Local development overrides.
# Falls back to application.properties for everything else.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/cavista}
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}

logging.level.org.springframework=INFO
logging.level.org.example.cavista=DEBUG
