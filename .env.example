# Base URL for the Spring Boot backend.
# In dev, Vite proxies /api -> http://localhost:8080, so leaving this at /api/v1
# lets the browser hit the same origin and keeps CORS simple.
VITE_API_BASE_URL=/api/v1
